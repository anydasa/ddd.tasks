tactician:
  commandbus:
    default:
      middleware:
        - tactician.middleware.validator
        - tactician.middleware.doctrine
        - tactician.middleware.command_handler
    validator:
      middleware:
        - tactician.middleware.validator
